var cantidad = prompt("Ingrese la cantidad de palabras que desea ingresar:");
function ordenar(cantidad){
    texto = new Array(cantidad);
    for(var i = 0; i < cantidad; i++){
        var mensaje = prompt("Ingrese la cantidad de palabras que desea ingresar:");
        palabra.push(texto);
    }
        alert(palabra.sort());
    }
ordenar(cantidad);